// Reserved. The gallery no longer reads OHLC data — each stock's numbers live
// inside its story in /stories. Kept as an empty stub so the legacy
// fetch_setups.py pipeline still has somewhere to write if it is ever re-run.
const PRICES = {};
